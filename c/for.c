#include <stdio.h>

int main(){
    // for loop = repeats a section of code a limited amount of times
    // for ( index declaration and initiaicion, condition, increment)

    for (int index = 1; index <= 10 ; index++) {
        printf("Printing line %d\n", index);
    }

    return 0;
}